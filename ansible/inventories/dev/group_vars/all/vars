env: dev
region: us-east-1

version_tag: "{{ lookup('env','GITTAG') }}"

docker_email: '{{ secret_docker_email }}'
docker_password: '{{ secret_docker_password }}'
docker_user: '{{ secret_docker_user }}'

dd_api_key: "{{ dd_apikey_encrypted }}"
dd_app_key: "{{ dd_appkey_encrypted }}"

splunk_host: "{{ secret_splunk_host }}"
splunk_token: "{{ secret_splunk_token }}"

manager_key: "{{ secret_manager_key }}"