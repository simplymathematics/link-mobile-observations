---
- name: Upload
  hosts: localhost
  tasks:
    - name: Log into Docker Hub
      docker_login:
        username: '{{ docker_user }}'
        password: '{{ docker_password }}'
        email: '{{ docker_email }}'
    - name: Upload image
      docker_image:
        name: intersection/link-mobile-observations:6832cf43ffdc5538a6ef87d3af09ff545067a1f2
    - name: Log out of Docker Hub
      docker_login:
        state: absent
